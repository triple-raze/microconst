## Aiogram example

This is a callback-based bot that heavily relies on callbacks,
which is good for showing advantages of the library.
Users should press inline keyboard buttons instead of typing commands

Main reason for using this library in telegram is 64-byte limit in callback data,
which significantly limits callbacks.

### Run
1. `pip install aiogram microconst`
2. `python main.py` 
